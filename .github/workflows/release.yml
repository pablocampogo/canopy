name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag for the release (e.g. v1.0.0)"
        required: true

permissions:
  contents: write

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Build binaries
        run: |
          make build/wallet
          make build/explorer
          mkdir -p dist
          GOARCH=amd64 CGO_ENABLED=0 GOOS=linux go build -a -o dist/cli-linux-amd64 ./cmd/main/...
          GOARCH=arm64 CGO_ENABLED=0 GOOS=linux go build -a -o dist/cli-linux-arm64 ./cmd/main/...
          GOARCH=arm64 CGO_ENABLED=0 GOOS=darwin go build -a -o dist/cli-darwin-arm64 ./cmd/main/...

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.inputs.tag }}
          target_commitish: ${{ github.sha }}
          files: |
            dist/cli-linux-amd64
            dist/cli-linux-arm64
            dist/cli-darwin-arm64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker-main:
    name: Docker - Main
    runs-on: ubuntu-latest
    needs: release

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Prepare tags
        id: prep
        run: |
          TAG=${{ github.event.inputs.tag }}
          DOCKER_TAG=${TAG//+/-}
          echo "docker_tag=${DOCKER_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./.docker/auto-update/
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            canopynetwork/canopy:${{ steps.prep.outputs.docker_tag }}
            canopynetwork/canopy:latest
          build-args: |
            BRANCH=${{ github.event.inputs.tag }}
            BUILD_PATH=cmd/cli

  docker-go:
    name: Docker - Go Plugin
    runs-on: ubuntu-latest
    needs: release

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Prepare tags
        id: prep
        run: |
          TAG=${{ github.event.inputs.tag }}
          DOCKER_TAG=${TAG//+/-}
          echo "docker_tag=${DOCKER_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./.docker/auto-update/
          file: ./.docker/auto-update/Dockerfile.go
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            canopynetwork/canopy:go-${{ steps.prep.outputs.docker_tag }}
            canopynetwork/canopy:go-latest
          build-args: |
            BRANCH=${{ github.event.inputs.tag }}
            BUILD_PATH=cmd/cli

  docker-python:
    name: Docker - Python Plugin
    runs-on: ubuntu-latest
    needs: release

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Prepare tags
        id: prep
        run: |
          TAG=${{ github.event.inputs.tag }}
          DOCKER_TAG=${TAG//+/-}
          echo "docker_tag=${DOCKER_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./.docker/auto-update/
          file: ./.docker/auto-update/Dockerfile.python
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            canopynetwork/canopy:python-${{ steps.prep.outputs.docker_tag }}
            canopynetwork/canopy:python-latest
          build-args: |
            BRANCH=${{ github.event.inputs.tag }}
            BUILD_PATH=cmd/cli

  docker-typescript:
    name: Docker - TypeScript Plugin
    runs-on: ubuntu-latest
    needs: release

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Prepare tags
        id: prep
        run: |
          TAG=${{ github.event.inputs.tag }}
          DOCKER_TAG=${TAG//+/-}
          echo "docker_tag=${DOCKER_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./.docker/auto-update/
          file: ./.docker/auto-update/Dockerfile.typescript
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            canopynetwork/canopy:typescript-${{ steps.prep.outputs.docker_tag }}
            canopynetwork/canopy:typescript-latest
          build-args: |
            BRANCH=${{ github.event.inputs.tag }}
            BUILD_PATH=cmd/cli

  docker-kotlin:
    name: Docker - Kotlin Plugin
    runs-on: ubuntu-latest
    needs: release

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Prepare tags
        id: prep
        run: |
          TAG=${{ github.event.inputs.tag }}
          DOCKER_TAG=${TAG//+/-}
          echo "docker_tag=${DOCKER_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./.docker/auto-update/
          file: ./.docker/auto-update/Dockerfile.kotlin
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            canopynetwork/canopy:kotlin-${{ steps.prep.outputs.docker_tag }}
            canopynetwork/canopy:kotlin-latest
          build-args: |
            BRANCH=${{ github.event.inputs.tag }}
            BUILD_PATH=cmd/cli

  docker-csharp:
    name: Docker - C# Plugin
    runs-on: ubuntu-latest
    needs: release

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Prepare tags
        id: prep
        run: |
          TAG=${{ github.event.inputs.tag }}
          DOCKER_TAG=${TAG//+/-}
          echo "docker_tag=${DOCKER_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./.docker/auto-update/
          file: ./.docker/auto-update/Dockerfile.csharp
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            canopynetwork/canopy:csharp-${{ steps.prep.outputs.docker_tag }}
            canopynetwork/canopy:csharp-latest
          build-args: |
            BRANCH=${{ github.event.inputs.tag }}
            BUILD_PATH=cmd/cli
